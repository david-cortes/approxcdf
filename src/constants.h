
#define EPS_BLOCK 1e-20
#define LOW_RHO 1e-9
constexpr const static double HIGH_RHO = 1. - 1e-3;

constexpr const static double inv_sqrt2_pi = 1. / std::sqrt(2. * M_PI);
constexpr const static double neg_inv_sqrt2 = -1. / std::sqrt(2.);
constexpr const static double inv_sqrt2 = 1. / std::sqrt(2.);
constexpr const static double sqrt2 = std::sqrt(2.);
constexpr static const double twoPI = 2. * M_PI;
constexpr static const double fourPI = 4. * M_PI;
constexpr static const double half_log_twoPI = 0.5 * std::log(twoPI);
constexpr static const double inv_fourPI = 1. / fourPI;
constexpr static const double sqrt_twoPI = std::sqrt(2. * M_PI);
constexpr static const double log_sqrt_twoPI = std::log(std::sqrt(2. * M_PI));
constexpr static const double minus_inv_twoPI = -1. / twoPI;
constexpr static const double inv3sqrt2pi = 1. / (3. * std::sqrt(2. * M_PI));
constexpr static const double asin_one = std::asin(1.);
constexpr static const double inv_four_asin_one = 1. / (4. * asin_one);
